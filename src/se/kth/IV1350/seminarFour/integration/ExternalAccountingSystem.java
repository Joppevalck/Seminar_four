package se.kth.IV1350.seminarFour.integration;

import se.kth.IV1350.seminarFour.model.CompletedSale;

/**
 * This is a external system for accounting, in this seminar this could be omitted.
 */
public class ExternalAccountingSystem {
    /**
     * Updates the accounting system.
     * @param completedSale is the completed sale that shall be accounted.
     */
    public void updateAccounting(CompletedSale completedSale){
        //Code that updates something
    }
}
